import { BaseEntity } from "../../shared";

export class RefStepPec implements BaseEntity {
    constructor(
        public id?: number,
        public label?: String,
        public code?: String,
  



    ) { }
}